# Production build env for bbacvs.kalootech.com (frontend only).
# NOTE: the BBACVS API is not deployed yet — the Verify feature will return an error until
# the api/ service is running and reachable. Point this at the API origin then rebuild.
NEXT_PUBLIC_API_URL=https://bbacvs.kalootech.com
NEXT_PUBLIC_CHAIN_ID=11155111
